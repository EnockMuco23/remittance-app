# Database migrations

No migrations have been added yet.

The live database existed before this directory was introduced. Do not create a
baseline migration by replaying inferred DDL against the existing project.
Await authoritative definitions before proposing any migration.
